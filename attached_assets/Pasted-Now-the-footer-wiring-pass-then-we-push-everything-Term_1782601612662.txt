Now the footer-wiring pass, then we push everything (Terms, Privacy, and the footer fixes) together. The About, Terms, and Privacy pages all exist now, so wire every footer link to its real destination across all pages: landing-login, blog, challenges, leaderboards, for-clubs, about, terms, privacy.
Wire each footer link BASE-aware using the /html/ href pattern (the shared script rewrites /html/* → /* on Railway):

About → href="/html/about" — and CRITICALLY, on landing-login REMOVE the old onclick="return scrollToSection('features')" that's intercepting the click and scrolling to the features section instead of navigating. Plain href, no onclick. (This is the scroll bug I reported.) Also fix the dead href="#" About links on blog, challenges, leaderboards to point to /html/about.
Terms → href="/html/terms" (replace the dead href="#" / onclick="return false" stubs everywhere)
Privacy → href="/html/privacy" (replace the dead stubs everywhere)
Contact → href="mailto:support@realarenas.com" (replace dead stubs; this opens the user's email client)

For for-clubs, which currently uses onclick="showToast(...)" toasts for Terms/Privacy — replace those with the real href links too.
Standardize each footer to show: About · Terms · Privacy · Contact. For the blog footer which also has a "Blog" link — since /blog currently redirects to /landing, either remove that dead Blog link or leave it; report what you chose. (Don't change blog routing, just the footer link.)
Verify on desktop and mobile: every footer link navigates correctly — About goes to the About PAGE (not scrolling), Terms → terms page, Privacy → privacy page, Contact opens a mailto. Confirm no layout breakage from removing the onclick handlers, and that links work BASE-aware (both /html/ on Replit and root on Railway). Report what you changed per file.
After this is verified, push EVERYTHING together to main — the Terms page, Privacy page, and all the footer wiring — in one push. Confirm the remote hash matches local HEAD so I know it all landed and Railway is redeploying.