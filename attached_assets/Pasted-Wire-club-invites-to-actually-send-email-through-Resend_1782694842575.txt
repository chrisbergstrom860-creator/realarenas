Wire club invites to actually send email through Resend. Go ahead with this plan:
1. Add the secret. RESEND_API_KEY read as process.env.RESEND_API_KEY. I'll add it in Replit Secrets (dev) and Railway Variables (prod) myself — set up the code to read it. Make the email helper degrade gracefully: if RESEND_API_KEY is missing, fall back to console.log (like supabaseAdmin is null without its key), so dev works without it and nothing crashes.
2. Create one shared sendEmail helper in server.js using plain fetch (no SDK — Node 24 has global fetch, keep deps minimal). It POSTs to https://api.resend.com/emails with Authorization: Bearer ${process.env.RESEND_API_KEY}, from "Arenas noreply@send.realarenas.com", and takes to/subject/html (and optionally text). Handle errors gracefully — log failures, don't crash the request (an invite DB row should still be created even if the email send fails; report how you handle a send failure). The helper must run in PRODUCTION (not behind the NODE_ENV !== 'production' guard the current console.logs use — that's the whole point).
3. Wire it in the 3 spots (single invite new-user branch ~4823, bulk new-user branch ~4908, resend ~4976) — send a real invite email with the joinUrl (which already uses publicBaseUrl so links resolve to the real domain). Write a clean, simple invite email (subject like "You're invited to join [club] on Arenas", a short friendly body with the join button/link). Use the club name and inviter where available.
4. Existing-user invitees: currently they get an in-app notification only. Change this so they get BOTH the in-app notification AND an invite email (so they notice even if they're not currently in the app). New users get the email (they have no in-app presence). So: everyone invited gets the email; existing users additionally keep the in-app notification.
5. Resend route: make it actually send the email (that's the point of resend). If it doesn't currently branch new-vs-existing, just email the invite to the address on the invite record.
Do NOT touch Supabase auth emails (confirmation/password reset) — those are a separate Supabase-SMTP task we'll do after. This task is only the app-level club invites.
Report your plan for the email HTML/content and how you handle send failures before sending, then build. Since you can't actually send without the key in your env, verify the code path is correct (the helper is called in all 3 spots, runs in prod, degrades gracefully without the key) — I'll do the real send-test once the key is added. Don't push until I confirm.