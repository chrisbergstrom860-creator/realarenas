Before building, one thing to confirm: the full notifications page had an "accept invite from a card" action (POST /api/clubs/:id/accept-invite). If we retire that page, can a user still accept a club invite somewhere else? Check whether club invites can be accepted from another surface (the /join/:token page, an email link, the club-invite flow, anywhere). If the notifications page was the ONLY place to accept an in-app invite, retiring it would strip that ability — report what you find before we proceed. If invites can be accepted elsewhere, we're clear to retire the page.
Assuming invites are handled elsewhere, build it with this plan:

Inject the notifications panel via the injectBottomNav chokepoint — same pattern as the bottom nav and managed-clubs menu. For any page whose bell has onclick="nav('/notifications')", rewrite that onclick to toggleNotificationsPanel() and insert the #notifications-panel markup as a child of the bell (anchoring to the bell's own position:relative — works for both icon-btn and notif-btn, no wrapper needed). Self-guard with the id="notifications-panel" check so it no-ops on the dashboard (which keeps its existing inline copy). Use replaceAll since all 8 bells share an identical signature.
Put the panel JS in a served file (html/arenas-notifications-panel.js) via a dual route like arenas.css (['/html/arenas-notifications-panel.js','/arenas-notifications-panel.js']), and <script src> it from the same injection step. This is the dashboard's panel logic — loadNotificationsPanel, toggleNotificationsPanel, renderNotificationsList, showAllNotifications, markNotificationRead, markAllNotificationsRead, the dot-sync IIFE, the outside-click close. Reuse it as-is so behavior matches the dashboard exactly.
Leave the dashboard's inline copy alone — do NOT unify it. It works; the injection no-ops there via the guard. We accept the dashboard keeping its own identical copy as the lower-risk choice.
Flip the /notifications route in server.js to redirect: app.get(BASE + '/notifications', (req, res) => res.redirect(BASE + '/feed')) — drop requirePageAuth. Keep all /api/notifications* routes (the dropdown needs GET, read, read-all). The arenas-notifications.html page file can stay on disk unused, or note it as retired.
The dot-sync script injected onto these pages also fixes a bonus bug — the .notif-dot is currently permanently visible on all 8 pages (no display:none default); the sync script will correctly show/hide it based on real unread count.

Report the accept-invite finding first. Then build, and verify on both desktop and 375px mobile: the bell opens the dropdown in place (no navigation), mark-as-read and mark-all work, "See all" expands in place, the unread dot reflects real count, and old /notifications links redirect to /feed. Don't push until I confirm.