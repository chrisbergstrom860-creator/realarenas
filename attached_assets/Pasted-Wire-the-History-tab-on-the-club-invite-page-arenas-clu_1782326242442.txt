Wire the History tab on the club-invite page (arenas-club-invite.html) to real data. The full invite array (window.INVITE_DATA.invites — accepted + pending + expired, with status/created_at/accepted_at/expires_at/isOpen/isExpired) is already injected and already consumed by renderPending, so the data is right there. Build a renderHistory function (parallel to renderPending) that runs on load and after refresh(). Here's the spec:
Stat cards (the .grid-4 row, L626–630) — compute from the real invites array, EXCLUDING open shareable links (isOpen), same as renderPending filters them out:

Total sent → count of real (non-open) invites. Label it "Total sent" (it reflects current invite records — revoked invites are hard-deleted so they won't be counted, which is fine; don't label it as a permanent all-time audit).
Accepted → count where status==='accepted'. Replace the fake "78% rate" delta with the real percentage (accepted / total, rounded).
Pending → already wired via setStat; replace the fake "3 expire soon" delta with a real count of pending invites expiring soon (isExpired-soon, or within N days of expires_at — use a sensible threshold).
Card 4 → RELABEL from "Declined/expired" to "Expired" only (there is NO decline mechanism in the backend, so "Declined" is fabricated). Count where isExpired. Replace the fake "10%" with the real percentage (expired / total).

History table (.hist-table, L634) — replace the 6 hardcoded sample rows with real rows mapped from the invites array (excluding isOpen sentinel records), like renderPending does:

Member → email-derived name (invites only store an email, so use the email or email-derived display like the pending table — NOT fake styled names like "Sofia L.")
Role → the invite's role
Sent → created_at (formatted)
Responded → accepted_at if accepted, else "—" (or "Expired" for expired ones)
Status → Accepted / Pending / Expired (no "Declined")
Time to accept → accepted_at − created_at for accepted invites, else "—"
Honest empty state if there are no invites yet ("No invites sent yet").

Remove the dead stubs (they're toast-only and making them real is separate feature work):

The "Export" button (L623), "Load all" (L646), and the "Showing 6 of 62 invitations" footer (L646) — remove these, or if you keep the footer, make it show the real count. No fake "62". Lean toward removing the export/load-all buttons since they don't work; report what you did.

Make sure renderHistory runs both on initial load AND after refresh() (after sending/revoking invites), so the history and stats stay current — same lifecycle as renderPending. And confirm it works for a real club manager (INVITE_DATA injected); the static non-manager mockup can keep showing placeholder, that's fine.
Since this is auth-gated, use the mock-harness screenshot approach (like you did for the RSVP widget and the mobile pass) to verify the History tab renders correctly with real-shaped data — stat cards computing right, table rows mapping correctly, empty state, and the "Expired" relabel. Verify desktop and mobile (the History table is the horizontal-scroll one on mobile). Report what you verified before pushing. Don't push until I confirm.