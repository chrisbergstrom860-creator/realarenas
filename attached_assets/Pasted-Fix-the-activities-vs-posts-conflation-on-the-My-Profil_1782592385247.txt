Fix the activities-vs-posts conflation on the My Profile page. The "Activities" count is wired to the posts table but should count actual activities (the activities table), which is what the list shows and what the "Activities" label means. The correct number is 4 (real activities), not 3 (posts).

Fix the count: change the source so the "Activities" badge AND the header "Activities" stat reflect the real count of rows in the activities table for the user (a count:'exact' query on activities, eq user_id), not postCount. Add a distinct field (e.g. activityCount) rather than overloading postCount — because postCount is a real "number of posts" value that may be legitimately used elsewhere; don't break it. Inject activityCount and point both the header stat (line ~900) and the tab badge (lines ~905–906) at it.
Confirm postCount is still correct wherever it's genuinely used as a post count — don't break any real use of it. Report where postCount is still used after this change.
Fix the related conflation: the Overview tab's "Recent activities" card is fed by the posts table, not activities (server fetches postsRes from posts, rendered into the "Recent activities" card). Decide and report: should this card show real recent ACTIVITIES (from the activities table, matching its label), or is it actually meant to show recent feed posts? Given it's labeled "Recent activities," it should show real activities from the activities table — fix it to pull from activities (the same source the Activities tab list uses), so the card matches its label. If there's a reason it should stay as posts, report that instead and we'll rename it rather than rewire it.
Cross-check: after the fix, the header "Activities" stat, the "Activities" tab badge, the activities list, AND the Stats & PRs tab's activity count should ALL show the same number (your real activity count, 4). Confirm they all agree.
While checking: is this same posts-vs-activities confusion causing wrong "activity" numbers anywhere ELSE — the feed, leaderboards, the "Your week" widget, club stats? You don't need to fix those in this task, but report any other place where a count labeled "activities" is actually counting posts (or vice versa), so I know if there's a broader cleanup needed.

Do NOT touch the "· Pro" items (that's the separate pending Tier 2 cleanup). Report your plan before applying, then fix, verify on desktop and mobile that all four activity-count displays agree and the Overview "Recent activities" card shows real activities. Don't push until I confirm.