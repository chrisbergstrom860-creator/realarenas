There's a layout bug on the Challenges page (arenas-challenges.html), in the "My challenges" tab. On first load the challenges display correctly — stacked vertically in a single column, with a "Points breakdown" sidebar on the right. But when you click the "My challenges" tab, the layout changes: the challenge cards reflow into a horizontal row that runs sideways off the right edge of the screen. I want it to STAY in the vertical stacked single-column layout (the way it looks on first load), not switch to horizontal.
Investigate and report before changing anything:

The "My challenges" tab — what happens when it's clicked? Find the tab's click handler and the function that renders/re-renders the My challenges list. On first load the content is rendered one way (vertical), but clicking the tab appears to re-render it differently (horizontal). Report both code paths: how the content is laid out on initial page load, versus how it's rendered/re-laid-out when the My challenges tab is clicked.
The key question: what's the CSS difference between the two states? The vertical (correct) version likely uses a container with flex-direction:column or a single-column grid. The horizontal (broken) version likely switches to flex-direction:row or a multi-column/horizontal-scroll grid. Find the exact container and the class/style that differs between first-load and post-click. Report the container's CSS in both states.
Is the tab-click re-render applying a different container class than the initial render, or removing a class? It looks like clicking the tab swaps the layout container to a horizontal one. Identify exactly where the layout flips from column to row.
Confirm which is the INTENDED design — the vertical single-column stacked layout (with the points sidebar on the right) is what I want. So whatever the tab-click does that makes it horizontal is the bug; the fix is to make the tab-click render produce the same vertical layout as the initial load.

Report all four before making changes. Don't push until I confirm.