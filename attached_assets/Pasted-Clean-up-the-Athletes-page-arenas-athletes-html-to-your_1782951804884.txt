Clean up the Athletes page (arenas-athletes.html) to your suggested minimal real version, with these specific calls. The real athlete grid, follow buttons, grid/list toggle, and search all work — keep them. Here's the plan:
1. Remove all fabricated content:

The "84,000+ athletes" top badge (line 273). Keep the real "20 athletes" count (#athlete-count).
The ENTIRE right rail (lines ~795–892): "People you follow" fake personas, "Your network" fake stats, "Your network by sport", "Clubs to discover" — all fabricated. Remove the whole rail.
The dead fabricated recommended/nearby persona strips (~360–785) — they're already hidden by the overlay, but DELETE the markup so it can't reappear if data injection ever fails (you flagged this risk).
The "Hackney RC" filter pill (fake club, line 319).

2. Dead filters — wire the real ones, remove the rest:

KEEP and WIRE UP: Show → "All athletes" and "Following" (filter isFollowing===true — trivial, data's already client-side). Make these actually filter the real grid.
KEEP and WIRE UP: a real Sort dropdown with options that work — Followers (data present), Name (A–Z), and "New to Arenas" (add created_at from listUsers server-side if needed, one line). Make the sort actually reorder the real grid.
REMOVE (dead, no real data / no engine): Level filters (Advanced/Intermediate/Beginner — mostly null data), Location filters (Near me/London/UK/Global — no geo), Activity filters (this week/month/all time — needs backend), Show → "Followers" and "Recommended" (need backend/engine). Remove these filter groups entirely rather than leave them dead.

3. Layout: with the right rail removed, let the athlete grid use the full width (like we did on the invite page cleanup). Make it look intentional, not empty. Responsive as always.
The principle (same as the invite page): every control that remains must actually work, every number must be real, nothing fabricated. After this, the Athletes page should be: real grid (full width) + working follow + grid/list toggle + search + real All/Following filter + real sort. Nothing fake, no dead controls.
Report your plan first — confirm exactly which filters you're keeping-and-wiring vs removing, and how you'll handle the sort — then make the edits. Verify the real grid, follow, toggle, search, and the newly-wired filters/sort all work. Show me a desktop and mobile screenshot before pushing. Don't push until I confirm.