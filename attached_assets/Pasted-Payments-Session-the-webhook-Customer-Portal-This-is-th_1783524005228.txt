Payments Session ③: the webhook + Customer Portal. This is the must-be-correct session — the webhook becomes the sole writer to the subscriptions table.
1. Webhook endpoint — POST /api/stripe/webhook:

Mount express.raw({ type: 'application/json' }) for this exact path BEFORE the global body parsers (the gotcha you banked in Session ①), and verify every request with stripe.webhooks.constructEvent using a new STRIPE_WEBHOOK_SECRET env var. Invalid signature → 400. If the Stripe client or secret is absent, log the skip and return 503.
Handle exactly four events; any other event type returns 200 and is ignored:

checkout.session.completed: retrieve the subscription, read owner_type/owner_id from metadata, and upsert on unique(owner_type, owner_id) — setting plan (derive from the price ID), stripe_customer_id, stripe_subscription_id, status, current_period_end, cancel_at_period_end.
customer.subscription.updated: identify the owner from the subscription's own metadata (the contract we set in Session ②); upsert status, current_period_end, cancel_at_period_end, and stripe_subscription_id. Upsert, not update — this event can arrive before checkout.session.completed, and out-of-order arrival must produce the same final row.
customer.subscription.deleted: set the row's status to canceled — do NOT delete the row (we keep stripe_customer_id for customer reuse; the plan helpers already treat non-active/non-past_due as free). Stale-event guard: only apply if the event's subscription ID matches the row's stripe_subscription_id — a delete for an old subscription must not downgrade an owner who has since re-subscribed under a new one.
invoice.payment_failed: set status to past_due, same stale-event guard via the invoice's subscription ID.


Every write updates updated_at. Return 200 promptly after the DB write; log and 500 on DB failure so Stripe retries.

2. Customer Portal — POST /api/billing/portal/pro (requireAuth) and POST /api/billing/portal/club/:clubId (requireAuth + the same manager gate as checkout): look up the owner's stripe_customer_id from subscriptions (404 with a clear message if none), create a Portal session with a publicBaseUrl() return URL, respond {url}.
3. Env + confirmation: add STRIPE_WEBHOOK_SECRET handling. For local testing use the Stripe CLI: stripe listen --forward-to localhost:<port>/api/stripe/webhook — the CLI prints a whsec_... secret; that's the dev value. Also confirm explicitly that the checkout success/cancel URLs are built by publicBaseUrl() at request time (not hardcoded to the dev domain) so they resolve to realarenas.com on Railway.
4. End-to-end test in test mode (fresh temp user + club, clean up after):

Complete a Pro checkout with 4242 → confirm the webhook fires, one row appears with plan pro, status active, correct period end, and getUserPlan now returns 'pro'.
Immediately retry Pro checkout → confirm the 409 guard now fires live (it couldn't in Session ②).
Club Pro checkout → row with plan club_pro; getClubPlan returns 'club_pro'.
Cancel the Pro subscription via the Customer Portal (immediate cancel, or use stripe trigger/dashboard cancel) → row flips to canceled, getUserPlan back to 'free', and a fresh Pro checkout then reuses the same stripe_customer_id (verify in Stripe — this closes Session ②'s duplicate-customer note).
Simulate invoice.payment_failed (Stripe CLI stripe trigger is fine) → status past_due, and confirm the plan helper still returns paid (grace).
Send a replayed/duplicate checkout.session.completed → confirm idempotency: still exactly one row per owner.

Report everything: the raw-body mount placement, each event handler's behavior, the full test matrix results, the final state of the table (should be back to clean), and anything unexpected. Touch nothing else.