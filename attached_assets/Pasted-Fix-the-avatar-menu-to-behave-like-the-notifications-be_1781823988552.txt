Fix the avatar menu to behave like the notifications bell panel: click-to-open, click-outside-to-close, no mouseleave. Use approach (b) — the shared injected script — so there's one source of truth and this can't drift across the 10 pages again.
Specifically:

Remove the onmouseleave close handler from the #userMenu wrapper on all pages. The cleanest way given it's inline per-page: in a shared script (injected by server.js the same way the notifications panel and managed-clubs menu are), neutralize/override the mouseleave behavior, OR if it's simpler, strip the onmouseleave attribute from the wrapper at serve time via the same injection chokepoint. Use whatever's cleanest given how the menu is injected — but the end state must be: no mouseleave-to-close on any page.
Add a document click-outside listener (in the shared script) that closes #userMenu when the click is outside both the menu and the avatar trigger — mirroring exactly what arenas-notifications-panel.js does for the bell panel (the listener at lines 169-177). Close when the click is outside both #userMenu and the .user-av avatar.
Keep click-to-open on the avatar as-is (the onclick toggle stays).
Make sure this shared script runs on all 10 pages that have the avatar menu (feed, my-profile, club-member, leaderboards, athletes, challenges, events, club-dashboard, club-invite, blog) — the same set where the menu exists. Note blog uses slightly different wrapper class names (topbar-user/avatar-sm) — make sure the click-outside logic targets the menu by its #userMenu id and the avatar by a selector that works across all pages, so blog isn't left out or broken.
Two dropdowns now share the click-outside pattern (bell + avatar). Make sure they don't interfere — opening one shouldn't break closing the other. Ideally, opening the avatar menu closes the bell panel and vice versa (only one open at a time is the expected behavior), but at minimum they must not conflict. Confirm how they coexist.

Verify on desktop: click the avatar → menu opens and STAYS open as you move the mouse into it → you can click My profile or Log out without it disappearing → clicking outside closes it. And the managed-clubs section (for coaches) is reachable the same way. Also verify on mobile at 375px — tapping the avatar opens the menu and items are tappable. Don't push until I confirm.