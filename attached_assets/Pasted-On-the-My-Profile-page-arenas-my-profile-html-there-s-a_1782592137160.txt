On the My Profile page (arenas-my-profile.html), there's a count mismatch. The "Activities" tab badge shows "3" and the header stat strip shows "3 ACTIVITIES", but the actual activities list in the Activities tab shows 4 activities (Evening run, Strong push, "time to do this", Early morning run). The count says 3 but there are 4 in the list. Investigate and report — don't change anything yet:

Where does the "Activities" count come from (both the tab badge and the header stat)? Earlier investigation showed the header "Activities" stat is wired to data.postCount. Is the activities count using postCount (count of POSTS) while the activities LIST shows actual logged activities — and are those two different things? That's my suspicion: the count is counting posts, but the list is showing activities, and they're not the same set.
What does the activities LIST actually render from — the user's logged activities (the activities table), or posts, or something else? Report the data source for the list versus the data source for the count.
The core question: are "activities" and "posts" two different things in this app that are being conflated? For example, does logging an activity create an activity record, while posting to the feed creates a post record — and the count is counting one while the list shows the other? Report how activities vs posts relate in the data model.
Which number is correct — should it be 3 or 4? Count the user's real activities (from whatever the list renders) versus what the badge/stat counts. Report the actual real count of the user's activities, so we know whether the badge is undercounting or the list is showing something extra.
Are the tab badge (next to "Activities") and the header stat ("3 ACTIVITIES") sourced from the SAME value, or computed separately? If separately, that's another place they could drift.

Report all five before changing anything. The fix is to make the count match the actual number of activities the user has — but I want to understand why they disagree first.
Also while you're looking: the "💳 Pro plan" sidebar nav item is visible in this screenshot — that's part of the pending "· Pro" cleanup (Tier 2). Don't fix it in this task, just confirm it's on the list.