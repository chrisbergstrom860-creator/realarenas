Payments Session ④ — the closeout: pricing honesty, real upgrade CTAs, billing page, and CA Automatic Renewal Law disclosures. Payments infrastructure is deployed and smoke-tested on production; this session makes it visible and honest.
1. Landing pricing section honesty pass (arenas-landing-login.html, ~line 805) — same "either real or absent, never fake" standard as the /for-clubs pass:

Pro tier: price becomes $9/month, monthly-only — delete the "Billed annually — or $12/mo monthly" copy (the only $12 in the repo).
Club tier: flat $29/month — delete "$1/mo per additional member"; rename to "Club Pro" and align its feature list with /for-clubs (which is already honest).
Delete ALL trial language: "Start Pro free for 14 days →", "Start Club trial", "All plans include a 14-day free trial" (~line 880). No trials exist.
Phantom features: change "5 AI coach analyses / Unlimited AI coach analyses" to an honestly labeled "AI coaching — coming soon" (same treatment as /for-clubs). Remove entirely: the 50-athlete follow cap, "Priority event matching", "Export your data anytime", "Recovery scoring", "Bulk event sign-ups", "Everything in Pro for all members". Keep only real features: feed, event discovery/RSVP, leaderboards, challenges, training analytics, training load.
Add the founding-period note to the paid tiers, consistent with the club wizard's existing "Free during the founding period" message.

2. Real upgrade CTAs: landing Pro CTA and /for-clubs Club Pro CTAs become upgrade-aware: logged-out → signup (current behavior); logged-in → POST to the respective checkout endpoint and redirect to the returned Stripe URL. For the club endpoint, use the caller's managed club; if they manage none, route to club creation.
3. Billing page + nav: build a billing page/section (behind requirePageAuth) showing current plan via getUserPlan (and getClubPlan for clubs the user manages), with: upgrade buttons (calling the checkout endpoints) when free, and a "Manage billing" button (calling the portal endpoints) when subscribed. Restore the "Billing" nav item as a real link to it. During founding period, the page states plainly that paid features aren't being charged yet.
4. CA Automatic Renewal Law disclosures — required, not optional. Immediately adjacent to every upgrade button (landing, /for-clubs, billing page), in clear and conspicuous text before checkout begins: the subscription renews automatically each month at $9/month (or $29/month), continues until canceled, and can be canceled anytime via Manage billing (Stripe portal). Nothing may imply a trial or one-time payment.
5. Banked bug: investigate and fix the buildCalendar TypeError (null appendChild) at my-profile ~line 784 — calendar code executes on a page state missing its target element. Guard or fix the root cause; report which.
Test: logged-out CTAs → signup; logged-in Pro CTA → real Stripe checkout (test mode, don't complete); billing page shows free state + disclosures; verify zero remaining instances in the repo of "$12", "annual", "trial", "14-day", per-member pricing, and the removed phantom features. Report everything, touch nothing else.