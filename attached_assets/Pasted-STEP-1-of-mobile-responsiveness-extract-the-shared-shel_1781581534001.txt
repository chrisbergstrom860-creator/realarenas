STEP 1 of mobile responsiveness — extract the shared shell CSS into one stylesheet with ZERO visual change. This is a pure refactor; the desktop layout must look byte-for-byte identical afterward. Do NOT add any mobile/responsive rules in this step.
Here's the plan:

Create a new file artifacts/html-arenas/html/arenas.css.
Identify the shell CSS that is duplicated across the app-shell pages (the 10 pages that use the .app/.topbar/.sidebar grid: arenas-feed.html, arenas-my-profile.html, arenas-events.html, arenas-challenges.html, arenas-leaderboards.html, arenas-athletes.html, arenas-notifications.html, arenas-club-dashboard.html, arenas-club-member.html, arenas-blog.html). This is the common shell: :root variables (--sidebar-w etc.), .app, .topbar, .topbar-logo, .topbar-search, .sidebar, .nav-item, .nav-section-label, and any other shell/chrome rules that are identical (or meant to be identical) across these pages.
Before extracting, compare the copies across these 10 files and report any DRIFT you find — places where the "same" rule has different values (e.g. --sidebar-w:220px vs 220px spacing, right rails 296px vs 300px, any other inconsistencies). List the drift so I can decide the canonical value for each. Do NOT resolve drift silently — report it and I'll confirm the canonical values. For genuinely page-specific CSS (like each page's .main inner grid, right-rail widths, page-specific components), leave those in the page's own <style> block — only extract what's truly shared shell.
Once we agree the canonical values, put the shared shell rules into arenas.css, add <link rel="stylesheet" href="/html/arenas.css"> (adjust path to match how the app serves static files — confirm whether it's /html/ or root based on the BASE path setup) into the <head> of all 10 shell pages AFTER the Google Fonts link, and remove the now-duplicated shell rules from each page's inline <style>.
Make sure server.js serves the new arenas.css file as a static asset (check how other static files are served and match that).

IMPORTANT: Stop after step 3 and report the drift findings BEFORE extracting anything. I want to confirm the canonical values for any drifted rules so we don't bake in a wrong value or accidentally change the desktop appearance.