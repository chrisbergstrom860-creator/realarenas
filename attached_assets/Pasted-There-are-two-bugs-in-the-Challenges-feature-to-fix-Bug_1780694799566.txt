There are two bugs in the Challenges feature to fix:
Bug 1 — Private challenge showing as completed with goal achieved even though no activities have been logged
This is happening because the progress calculation is dividing by zero or null. Please add a safety check in the progress percentage calculation everywhere it appears in server.js and arenas-challenges.html:
javascript// Safe percentage calculation — never divide by zero
const pct = c.goal_target && c.goal_target > 0
  ? Math.min(100, Math.round((c.progress / c.goal_target) * 100))
  : 0;
Also check the isExpired and completed logic. A challenge should only appear in the completed section if BOTH conditions are true:

c.isExpired === true (end date has passed) AND/OR
c.progress >= c.goal_target AND c.goal_target > 0

Update the completed filter in the renderMyChallenges function in arenas-challenges.html:
javascriptconst active = challenges.filter(c =>
  !c.isExpired && !(c.goal_target > 0 && c.progress >= c.goal_target)
);
const completed = challenges.filter(c =>
  c.isExpired || (c.goal_target > 0 && c.progress >= c.goal_target)
);
Bug 2 — Check that goal_target is being saved correctly to Supabase
Please add a console log to the /api/challenges/create route to confirm what values are being inserted:
javascriptconsole.log('Creating challenge with goal_target:', req.body.goal_target, typeof req.body.goal_target);
Also make sure the insert converts goal_target to a number:
javascriptgoal_target: parseFloat(req.body.goal_target) || 0,
Bug 3 — Also check the enrich function in the /api/challenges route
In the enrich function in server.js add the same safety check:
javascriptfunction enrich(challenges) {
  return (challenges || []).map(c => {
    const participants = (allParticipants || []).filter(p => p.challenge_id === c.id);
    const isJoined = myIds.includes(c.id) || c.created_by === userId;
    const daysLeft = Math.max(0, Math.ceil(
      (new Date(c.end_date) - new Date()) / (1000 * 60 * 60 * 24)
    ));
    const goalTarget = parseFloat(c.goal_target) || 0;
    const progress = parseFloat(progressMap[c.id] || 0);
    const pct = goalTarget > 0 ? Math.min(100, Math.round((progress / goalTarget) * 100)) : 0;
    return {
      ...c,
      goal_target: goalTarget,
      participantCount: participants.length,
      isJoined,
      progress,
      pct,
      daysLeft,
      isExpired: new Date(c.end_date) < new Date(),
      isComplete: goalTarget > 0 && progress >= goalTarget,
      isOwner: c.created_by === userId
    };
  });
}
Then update the buildChallengeCard function in arenas-challenges.html to use c.pct and c.isComplete instead of recalculating:
javascript// Replace the pct calculation line with:
const pct = c.pct || 0;
const isCompleted = c.isExpired || c.isComplete;
And update the completed filter:
javascriptconst active = challenges.filter(c => !c.isExpired && !c.isComplete);
const completed = challenges.filter(c => c.isExpired || c.isComplete);
Also go to Supabase → Table Editor → challenges and check the goal_target column for the private challenge that showed as completed. Tell me what value is stored there — if it is 0 or null that confirms the bug.
Wait for checkpoint then commit and push to GitHub main immediately.