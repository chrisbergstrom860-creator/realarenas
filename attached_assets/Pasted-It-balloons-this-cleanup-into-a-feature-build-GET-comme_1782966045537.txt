It balloons this cleanup into a feature-build (GET-comments endpoint, real fetch/post wiring, comment rendering, verification) — mixing "remove the fake" with "build a new feature" in one pass, which is how things get messy.
Comments are a real feature worth building properly as its own focused task — not bolted onto a cleanup. Building it hastily to avoid removing the fake version is backwards.
The honest cleanup ships now; real comments come later, done right.

So: remove the fake comment modal and the fake Share button (which also does nothing — toasts "link copied" but copies nothing, no permalink exists). Keep the real Like button (that one works). This makes posts honest: you can like a real post (works), and comments/share come later as real features.
Note comments + share as real future features — like personalized invites and auto-reminders, these are legitimately good things to build properly later. Removing the fake versions now doesn't burn them; it just stops lying about them.
And Replit's other discovery — the orphaned notifications modal (nothing even opens it; real notifications come from the injected panel we built earlier) — remove it entirely. Pure dead fake content.
Tell Replit:

Go with Option A — remove the fake comment UI. Reasoning: comments don't actually work (fake hardcoded comments, the post button just toasts, no GET-comments endpoint), so it's fabrication that also discards real user input. Real comments are worth building properly later as their own task, not bolted onto this cleanup.
So, the full plan, confirmed:

Remove the "Your AI Coach" box (364-380) + dead dismissNudge().
Remove all 11 fabricated cards (382-666) + the dead "Load more" stub + orphaned helpers (toggleLike, rsvpGoing). Keep all real rendering (renderInjectedPosts/Activities/RSVPs), composer, and real Like functionality intact.
Remove the dead "Latest first" sort dropdown (323).
Filter pills — make them real, exactly as you planned: rename to All/Running/Cycling/Climbing/Clubs only, remove the ✦ AI insights pill, rewrite setFilter() to drop the buggy always-show clause, and tag every real card with its true type on render (posts → post.sport or "note" for sportless notes; activities → activity.sport; RSVPs → "clubs"; new composer posts → same as posts). Sports without a pill show under All only — that's fine.
Empty states — add both honest states as you described: "Your feed is just getting started / Follow other athletes…" (follows nobody) and "No activity from the people you follow yet / When the athletes you follow share…" (follows people but no posts). Centralize in one coordinator after the render functions.
COMMENTS + SHARE (Option A): remove the fake comment modal (#modal-comment, 729-757) and the fake Share button on posts (it copies nothing, no permalink exists). Keep the real Like button. Note: real comments and share are good future features to build properly later — just not shipping the fake versions now.
Remove the orphaned notifications modal (#modal-notifications, 759-793) — nothing opens it, real notifications come from the injected panel. Pure dead fake content.

The principle: every control that remains works, nothing fabricated, honest empty states cover the no-content cases. After this: composer (real) + real posts/activities/RSVPs + real Likes + working sport filters + honest empty states. All fake people, fake AI, fake comments/notifications gone.
This is a big removal — same care as the Athletes cleanup (which had a splice that threw and hid a real bug). After editing, VERIFY thoroughly: file parses (node --check all script blocks), HTML tags balanced, NO dangling references to any removed helper/modal/card, real rendering + composer + likes + the newly-wired filters all intact. Report the verification results, then show me desktop + mobile screenshots (populated with real-shaped data if you can mock it, AND both empty states). Don't push until I confirm.