Do a full per-page mobile responsive pass on arenas-club-invite.html (standalone page, own inline <style>, no arenas.css, no bottom nav — same kind of standalone job as landing/for-clubs, but bigger). Add a @media (max-width:768px) block (and adjust the existing one) to its inline styles. Here's the plan:
Layout shell:

Collapse .app from grid-template-columns: var(--sidebar-w) 1fr to a single column on mobile. Hide the 220px sidebar (display:none) — its nav isn't essential for the invite flow on mobile, and there's no bottom nav on this page. (If you think a back-to-dashboard link is needed since the sidebar is hidden, add a simple one in the topbar — report if you do.)
Fix the topbar: drop the fixed 220px .topbar-logo width, let the topbar wrap or shrink so the club name, the Coach/Admin pill, bell, and avatar all fit at 375px without clipping. The bell and avatar must stay reachable (notifications panel already works).
The body{overflow-x:hidden} currently CLIPS overflowing content (making it untappable). Reconsider this — either remove it on mobile or ensure nothing overflows the viewport, so no controls get cut off. The goal: nothing important is clipped off-screen.

Grids — collapse all to single (or 2-col where sensible) on mobile:

4. .grid-4 stat rows (Overview/Invite/History, repeat(4,1fr)) → 2×2 on mobile (not 4-across).

5. .grid-2 two-column workflows (invite form + email preview L299; CSV import L491) → single column, stacked.

6. The inline 3-col grids — .csv-row/.csv-header-row, CSV results tiles, .send-summary in the success modal — collapse to single or 2-col so they don't clip.

7. The email preview card (.ep-body, heavy padding) → reduce padding on mobile so it's readable when stacked.
Tables — this is the important part, handle per type:

8. STACKED CARDS on mobile for the ACTION tables — the pending-invites table (.pending-table, rendered by renderPending) and the JS-injected members table (4-col with role select + Remove). On mobile, each row should become a card: the key info (email/name, status, date) stacked vertically, and the action buttons (Resend/Revoke for pending; role select + Remove for members) shown clearly below each row, easy to tap. This is so a coach can actually manage invites/members from their phone without horizontal scrolling to reach buttons.

9. HORIZONTAL SCROLL for the read-only history table (.hist-table) — wrap it in a scroll container (overflow-x:auto) so it scrolls sideways on mobile. It's reference-only, no action buttons, so scroll is fine.

10. The invite-entry table (.inv-table — the email + first-name + role + delete rows for composing invites) → this one a coach actively uses to enter emails, so make it usable: stack each invite-entry row vertically on mobile (email field full width, then name, role, delete) so they can actually type. Don't leave it as a crushed 4-col table.
Core interactive controls — must be usable at 375px:

11. The shareable join link (JS-injected: readonly input + Copy + Generate, in a flex row) → make it wrap/stack so the input is full-width and the Copy/Generate buttons are tappable below it, not crushed.

12. The single-email invite field and "+ Add another email" → usable full-width (covered by #10).

13. The bulk/paste-list modal and CSV import → ensure the modal textarea and the import flow are usable on mobile.
CRITICAL — cover the JS-injected content: the join link (L804) and the members table (L803) are injected by JS at runtime, NOT in the static HTML. Make sure your mobile CSS rules target the classes those injected elements use (they reuse .pending-table / .form-input per the investigation), so the injected content gets the responsive treatment too — not just the static markup. This is the easy-to-miss part.
Verify at 375px AND desktop (desktop must stay unchanged): the sidebar collapses, topbar fits, all grids stack, the pending-invites and members tables render as tappable stacked cards with reachable action buttons, the history table scrolls horizontally, the invite-entry rows are usable for typing emails, and the join-link copy works. Since this is auth-gated, use the mock-harness screenshot approach you used for the RSVP widget to visually confirm the key states (invite form, pending-invites cards, join link, members cards) at both widths. Report what you verified before pushing. Don't push until I confirm.