We've set up Resend for sending email (domain send.realarenas.com is verified, and I have a Resend API key to add as a secret). I want to wire the app to actually send emails through Resend instead of console.log-only. Before building, investigate and report — don't change anything yet:

Find every place the app currently tries to send an email. From earlier work, club invites were "console.log only" (printed, not sent). Find all of these: club invites (single, bulk, resend), and any other app-level emails. For each, report the file/line and what it currently does (console.log? nothing? a stubbed function?).
Is there an existing email-sending helper/function in the codebase (even a stub), or does each spot handle email inline? I want to know if there's one place to wire Resend, or several.
How does authentication email currently work — signup confirmation and password reset? These are typically handled by Supabase's auth system, not app code. Report: is email confirmation currently ON or OFF in the Supabase setup? Does Supabase send these through its own default email service, or is it configured with a custom SMTP provider? (I'm trying to understand whether auth emails go through Supabase separately from the app-level invite emails.)
Is there an ANTHROPIC_API_KEY-style secrets mechanism already in use (env vars / Replit Secrets / Railway env vars)? I'll be adding a RESEND_API_KEY secret — confirm where app secrets live and how the code reads them (process.env.X?), so we add it correctly in both Replit and Railway.
What Node setup is there for HTTP requests — is there a fetch/axios available, or would we use Resend's official SDK (npm install resend)? Report what's already available so we wire it cleanly.

Report all five before building. The plan will be: add the RESEND_API_KEY secret, create one shared sendEmail helper that calls Resend, and wire the club invites to actually send through it. Auth/confirmation emails (Supabase) are a separate piece we'll handle after.
Don't change anything yet — just investigate and report.