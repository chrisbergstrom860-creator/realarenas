Clean up the feed center column — it's a remove-the-fake job since the real feed data path already works. Here's the plan:
1. Remove the "Your AI Coach" yellow box entirely (lines ~365-380). It's 100% fabricated (fake training-load/HR/marathon analysis of data that doesn't exist), its buttons are stubs, and it misrepresents AI coaching which is an unbuilt planned feature. Remove it and its dead helper (dismissNudge).
2. Remove all 11 hardcoded fake demo cards — Sofia L., Sofia R., Marco C., Yuki T., Alena L.'s marathon build, the Hackney RC "London Half Marathon" club update (Coach Rachel / code HRC2026), the "Achievement unlocked" banner, the challenge leaderboard, and the Victoria Park Parkrun event card. All fake people, fake stats, fake AI analysis, fake kudos/comment counts. Remove them and their now-dead helpers (toggleLike, rsvpGoing). Keep the REAL rendering (renderInjectedPosts/renderInjectedActivities/renderFollowingRsvps) fully intact.
3. Remove the fake sample data in the modals — the fake sample comments in the comment modal (~line 744) and the fake notification items in the notif modal (~769-788). (Keep the real comment/notification functionality — just remove the hardcoded fake samples.)
4. Remove the dead "Latest first" sort dropdown (~line 323, onchange just toasts — sorts nothing).
5. Filter pills — make them real, don't remove:

KEEP and WIRE UP: Running / Cycling / Climbing / Clubs only. The setFilter() mechanism already works; the problem is every injected real card is tagged data-type="all". Fix this: tag each real injected card (posts + activities) with its actual sport/type so the filters genuinely narrow the real feed. Keep "All" too.
REMOVE: the "✦ AI insights" pill (it only exists to surface the fake AI nudge; AI coaching isn't built, so there's nothing real to filter).

6. Fix the empty-state gap: currently followsNobody only fires when you follow nobody. But if you follow people who haven't posted, the feed renders completely empty with no message. Add an honest empty state for that case — e.g. "No activity from the people you follow yet" (distinct from the "follow nobody / discover athletes" banner). So the feed always shows something honest: real posts if they exist, or the appropriate empty message.
The principle (same as Athletes/invite): every control that remains must work, nothing fabricated, and the honest empty states cover the no-content cases. After this: composer (real) + real posts/activities/RSVPs + working sport filters + real likes/comments + honest empty states. All fake people, fake AI, fake stats gone.
This is a big multi-part pass — report your plan first (especially how you'll tag the real cards for the filters, and the empty-state wording), then make the edits. Since /feed is auth-gated, verify via code + a temp render harness, and show me desktop + mobile screenshots (both a populated state if you can mock real-shaped data, and the empty state). Don't push until I confirm.