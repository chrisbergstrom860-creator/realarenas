Go through the feed right-rail widgets with these decisions. The principle: wire to real data where the backend exists and it's useful, remove where "making it real" needs a feature build, strip all fabricated content, and give each wired widget a sensible empty/low-data state since this is a new app.
First, confirm what real data is available to the feed page — does the /feed route inject (or can it easily inject) into window.ARENAS_DATA: the user's weekly km, current streak, club rank, and a list of suggested users to follow (real users they don't already follow)? Report what's available now and what would need adding to the route. I want to know the data cost before wiring.
Then:

Your week — WIRE to real data: this week's km (sum from activities), club rank (from the leaderboard system). Remove the fabricated "540 pts to overtake Priya R." line entirely. Empty/low-data state: if the user has 0 activities this week, show something honest like "No activities logged this week yet" rather than fake numbers or a broken-looking 0.
Hidden mini-leaderboard (display:none, lines ~728) — DELETE entirely. It's invisible, fabricated, and redundant with /leaderboards.
Activity streak — WIRE to the real current streak (already computed for achievements) and real this-week day dots (which days this week had activity). Remove the fabricated per-day tooltips. Empty state: if streak is 0, show an encouraging "Start your streak — log an activity today" rather than fake "6 days."
Athletes to follow — WIRE to real users the person doesn't already follow, with working Follow buttons that call the real follow API (POST /api/follow/:userId), not the cosmetic toast. Empty state: if there are no suggestions (few users, or already following everyone), HIDE the widget entirely rather than showing fake people. Report how you'd source the suggestions (e.g. recent signups not yet followed, or club-mates not yet followed).
Quick actions — FIX: keep "View challenges" and "Upcoming events" (they navigate for real) but REMOVE their fabricated subtitles ("3 active · rank #5", "London Half · Apr 13") — either no subtitle or a real one. Change "Log an activity" to navigate to the real logger (/profile#activities) instead of opening the fake modal. REMOVE the "AI coach insights" row entirely (it's dead, no destination — we'll add it back when AI coaching is built).
Trending topics — REMOVE entirely. No hashtag backend exists, the counts are fabricated, and building a trending system is a feature, not a wire.

Report the data-availability findings (the "first" question) and your plan for sourcing the "athletes to follow" suggestions BEFORE building. Then apply, give each wired widget a proper empty state, verify desktop and mobile, and confirm no fabricated numbers/users remain in the right rail. Don't push until I confirm.