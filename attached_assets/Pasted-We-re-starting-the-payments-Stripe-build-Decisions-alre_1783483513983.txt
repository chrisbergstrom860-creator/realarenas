We're starting the payments/Stripe build. Decisions already made: Individual Pro is $9/month, monthly-only (no annual option — the pricing page's annual/$12 copy is wrong and will be corrected in this build). Club Pro is $29/month, monthly-only, purchased by the club's coach/admin on behalf of the club. Everything will be built and tested in Stripe test mode only — no live charging in this phase.
Before any code, investigate and report the current state — don't change anything:

THE PLAN DATA MODEL: Confirm there's no plan/subscription column anywhere (users table metadata, clubs table). Report what schema additions would be needed to track: a user's plan (free/pro), a club's plan (free/club_pro), Stripe customer IDs, and subscription status/period. Propose the minimal schema — likely stripe_customer_id + plan + subscription fields, or a dedicated subscriptions table. Recommend the cleanest approach given the Supabase setup, keeping in mind both users AND clubs can hold subscriptions.
THE PRICING PAGES: Inventory everything the pricing/marketing pages currently promise per tier (landing pricing section, /for-clubs). List exactly what features each tier claims (Free vs Pro vs Club Pro) so we can check each claim against what actually exists today. Flag every promised feature that does NOT exist yet (e.g. AI coach analyses). Flag all instances of the outdated annual/$12 pricing copy that must change to $9/month monthly-only. Re-confirm the current state of the /for-clubs order-summary flow.
WHERE GATING WOULD LIVE: If Pro/Club Pro gated features, where would enforcement go? Report the shape of it (middleware/checks on specific endpoints) — not building yet, just mapping.
THE UPGRADE ENTRY POINTS: Where would users start an upgrade? What do the pricing-page CTAs currently do? Note that the removed "Pro plan"/"Billing" nav items will eventually return as real links. Report current CTA behavior on all pricing surfaces.
STRIPE INTEGRATION SHAPE: Given this Express + Supabase + Railway stack, report the standard integration plan: Stripe Checkout (hosted) vs custom; webhook endpoint needs (which events matter: checkout.session.completed, customer.subscription.updated/deleted, invoice.payment_failed); where STRIPE_SECRET_KEY and the webhook signing secret live (env vars — set independently in both Replit and Railway, they share no secrets); and Stripe Customer Portal for self-serve cancellation. Recommend the simplest robust architecture — expected shape: hosted Checkout + webhooks updating our plan data + Customer Portal. Confirm or propose better.

Report all five before we build anything. This is a multi-session build; this first session is mapping only.