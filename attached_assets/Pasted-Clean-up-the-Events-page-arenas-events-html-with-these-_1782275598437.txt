Clean up the Events page (arenas-events.html) with these decisions. Principle: remove visible fabricated/dead content, keep the real wired widgets, and delete the hidden dead mock code while we're in here.
REMOVE (visible fabricated/dead):

The "AI Coach Recommendation" box (lines 680–684) — entirely. It's fabricated and AI coaching isn't built. If removing it leaves a gap at the top of the right rail, close it cleanly so "Your RSVPs" moves up.
The "2,100 events live" counter — both the visible one (line 265) and the hidden one (line 438).
The Challenges nav badge "3" (line 286) — remove the fabricated count. If there's a real active-challenge count easily available, wire it; otherwise just remove the badge.
The "· Pro" identity suffix (line 296) — remove it. There's no plan system, so it's fiction for every user.

FIX:

5. The "Manage all →" link (line 690) — point it at /profile (which has real "My upcoming RSVPs"). If wiring cleanly is awkward, just remove it since the RSVPs are already listed right below it. Your call on which is cleaner — report which you did.

6. The header search (line 263) — it currently filters the wrong element class (.ev-card) so it does nothing against real events (.evx-card). Either wire it to filter the real event cards if that's a small change, OR remove the search bar entirely. Given the page may show few events, lean toward removing it unless wiring is trivial. Report which you chose and why.
DELETE (hidden dead mock code — safe cleanup):

7. Remove all the hidden/unreachable mock code still sitting in the file: the filter zone (id=filter-zone, 305–353), the featured card (id=featured-section, 362–403), the map view (id=map-view, 406–447), the "Near you this month" side-card (736–766), the "Hackney RC events" side-card (768–810), loadMore (1111–1147), the entire openEventModal/EVENTS_DATA block (828–1010+), and quickRsvp/featRsvp (1081, 1087). Also remove any now-unused CSS and any hideMockChrome logic that only existed to hide these (if hideMockChrome becomes empty/unnecessary after deletion, remove it too). Be careful NOT to touch the real wired pieces: renderMyRsvps, renderEvents/buildEventCard, the real Create event modal, the identity injection, and the My clubs injection.
LEAVE ALONE (confirmed real): Your RSVPs widget, events grid, Create event button, notification bell, avatar menu/dropdown, My clubs sidebar.
Report your plan before deleting the big code blocks (item 7) — specifically confirm that each block you're deleting is truly unreachable and that no real code path calls into it, so we don't break the working page. Then apply everything, verify desktop and mobile, and confirm: no fabricated content visible, no dead buttons, the real widgets still work (RSVPs render, Create event works, events grid loads), and nothing broke from the code deletion. Don't push until I confirm.