On the landing page (arenas-landing-login.html), the "How it works" section has a 4-step flow: 1) Create your profile, 2) Connect Strava, 3) Get AI coaching, 4) Find your people. Strava integration doesn't exist yet, so step 2 "Connect Strava" promises a feature we don't have. Remove it — but reflow cleanly since the heading says "four steps."

Find the "How it works" section and report its structure — the heading ("Up and running in four steps"), the 4 step items, and how they're laid out (the grid/flex, the connector line between steps, the numbering).
Remove the "Connect Strava" step. Then reflow to a clean 3-step flow:

Renumber the remaining steps 1, 2, 3 (Create your profile → 1, Get AI coaching → 2, Find your people → 3)
Update the heading from "four steps" to "three steps"
Adjust the grid/columns so 3 steps lay out evenly (it was likely repeat(4,...) — make it 3), and make sure the connector line between steps still renders correctly with 3 items, not 4 (no dangling connector after the last step)


Replace step 1's description if it references Strava — it currently says "Pick your sports, set your level, and connect your location. Takes 90 seconds." Check it doesn't imply Strava; if it's fine, leave it.
While you're in here — search the WHOLE landing page (and the for-clubs page) for any OTHER mention of Strava, since the right-panel product preview or feature sections might also reference "automatic sync" or "no manual logging" which implies Strava. The hero subtext or feature cards may say things like "activities sync automatically." Report every Strava reference or "automatic sync / no manual logging" claim you find, because those are the same broken promise. We log activities manually right now, so any "automatic sync" claim is also false until Strava exists.

Report what you find in step 4 before applying changes — especially any "automatic sync" / "no manual logging" language, since that's the same problem hiding in different words. Then apply the 3-step reflow. Verify desktop and mobile, confirm the connector line looks right with 3 steps and there are no leftover Strava references. Don't push until I confirm.