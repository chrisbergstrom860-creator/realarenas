Make arenas-landing-login.html mobile responsive. Add ONE @media (max-width: 768px) block to its own <style> block (this page is self-contained). The strategy is: collapse every multi-column grid to single column, stack the two-column sections, shrink the large fixed fonts, and make the nav/footer wrap. Here's the block:
css@media (max-width: 768px) {
  /* Prevent any horizontal scroll from decorative elements */
  body { overflow-x: hidden; }

  /* Topbar nav — allow wrap, tighten */
  .topbar-inner { flex-wrap: wrap; gap: 12px; padding: 12px 16px; }
  .topnav { gap: 12px; flex-wrap: wrap; }

  /* Hero — stack to single column, shrink heading, reduce padding */
  .hero { grid-template-columns: 1fr !important; min-height: auto; }
  .hero-left { padding: 40px 20px !important; }
  .hero-h1 { font-size: 32px !important; line-height: 1.15; }
  .hero-right, .hero-left { width: auto; }

  /* All multi-column content grids → single or 2-col where it still reads */
  .stats-bar-inner { grid-template-columns: 1fr 1fr !important; gap: 16px; }
  .features-inner { grid-template-columns: 1fr !important; }
  .steps-inner, .steps-grid { grid-template-columns: 1fr !important; }
  .testimonials-inner, .testimonials-grid { grid-template-columns: 1fr !important; }
  .pricing-inner, .pricing-grid { grid-template-columns: 1fr !important; }

  /* For-clubs teaser 2-col → stack */
  .for-clubs-teaser, .teaser-inner { grid-template-columns: 1fr !important; gap: 32px !important; }

  /* Section titles and big numbers shrink */
  .section-title { font-size: 26px !important; }
  .stat-num { font-size: 26px !important; }
  .pricing-amount, .amount { font-size: 30px !important; }
  .cta-band h2, .cta-title { font-size: 26px !important; }

  /* The horizontal connector line on the steps section — hide on mobile */
  .steps-connector, .step-connector { display: none !important; }

  /* Footer — wrap to column */
  .footer-inner { flex-direction: column; gap: 24px; }

  /* AUTH SECTION (login/signup) — the important one */
  .auth-page { grid-template-columns: 1fr !important; min-height: auto; }
  /* Hide the dark branding panel on mobile, just show the form */
  .auth-left, .auth-branding { display: none !important; }
  .auth-form-wrap { max-width: 100% !important; padding: 32px 20px !important; }
  /* Stack the side-by-side name fields */
  .form-row { grid-template-columns: 1fr !important; }

  /* Clip the oversized decorative glows so they can't cause scroll */
  .cta-band::before, .auth-glow { max-width: 100vw; overflow: hidden; }
}
Two things to confirm as you apply it:

Check the actual class names against the file — I've guessed some (.steps-inner, .auth-left, .auth-branding, .teaser-inner) based on your report. Use the real class names where mine don't match, and tell me any I got wrong.
For the auth section — I chose to HIDE the dark branding panel on mobile and show only the form, so the login/signup form gets the full width instead of being crushed to half. Confirm that reads well — the form should be centered and full-width with the heading above it. If hiding the branding loses important context (like a tagline the user needs), tell me and we'll keep a slim version instead.

Apply it, then verify at 375px and report: does the hero stack and read well, do all the grids collapse, and critically — is the login/signup form usable and full-width? Don't push until I confirm from your report.