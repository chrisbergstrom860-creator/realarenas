We're making the athlete bell behave like the club dashboard bell — an in-place dropdown notifications panel instead of navigating to the full /notifications page. The plan is dropdown-only: retire the separate notifications page. Before changing anything, investigate and report:

The dashboard's notifications panel — report the complete implementation that we'd reuse: the panel markup (#notifications-panel and its contents), the toggle function (toggleNotificationsPanel), the function that loads/renders notifications into it, mark-as-read / mark-all-read handlers, the showAllNotifications expand behavior, and which /api endpoints it calls. I need the full picture of what makes the dashboard panel work so we can share it.
On the 7 athlete pages, the bell currently does onclick="nav('/notifications')". List each page and confirm that's the exact handler. Also report the bell's markup on those pages (the .notif-btn and any .notif-dot) so the panel can anchor to it the same way it does on the dashboard.
What links to /notifications anywhere in the app? Search all files for nav('/notifications') and href to /notifications. I need every inbound link — the athlete bells, but also anything else (the dashboard's old "See all" before we changed it, any notification-related buttons, deep links). This tells us what to redirect when we retire the page.
The /notifications route in server.js — what does it serve and what does it inject? If we retire the page, should the route redirect somewhere (e.g. to /feed) for any old bookmarks or external links, rather than 404? Report how it's set up.
Sharing approach: the dashboard panel's markup + JS currently live only in arenas-club-dashboard.html. To put it on all 7 athlete pages without pasting it into 7 files, what's the cleanest option given this codebase — inject the panel markup server-side (like we did for the bottom nav), put the shared JS in a served file, or something else? Recommend the approach that avoids duplication, consistent with how we've handled shared things before.

Report all five before we build. Don't change anything yet.