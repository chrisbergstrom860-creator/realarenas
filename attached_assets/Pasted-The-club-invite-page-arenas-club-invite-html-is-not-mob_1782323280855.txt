The club-invite page (arenas-club-invite.html) is not mobile responsive — when a coach opens "Invite members" on their phone, it renders the desktop layout which doesn't fit and isn't usable. This page was not included in the earlier mobile-responsiveness work (it was deferred along with club-join). Investigate and report — don't change anything yet:

Confirm arenas-club-invite.html is the page reached from "Invite members" in the club dashboard/club section. What route serves it and how does a coach get there?
Does this page use the app shell (the .app/.sidebar/.topbar grid that arenas.css styles), or is it a standalone layout? Does it link arenas.css? This determines whether the shared mobile rules already partially apply or whether it needs its own treatment.
Report its layout structure and what specifically breaks at 375px — the same kinds of things we fixed elsewhere: fixed-width sidebar forcing horizontal overflow, multi-column grids that don't collapse, fixed-width form elements, tables (the pending-invites table and bulk-email area), the invite-method cards. List each element that overflows or becomes unusable on a phone.
Specifically check the interactive parts a coach needs on mobile: the single email invite field, the bulk email input, the shareable join link (copy button), and the pending-invites management. Are these reachable and usable at 375px, or do they overflow/break? You mentioned the dropdown/notifications panel on this page earlier got the shared mobile fix — confirm what else does and doesn't work.
Is the fix the same pattern we used for the other shell pages (collapse the sidebar to the bottom nav, stack columns, make the grid single-column via the shared arenas.css mobile rules) — or does this page need its own standalone responsive treatment because it doesn't use the shell? Report which, so we know if it's a quick "it just needs the shared rules applied" fix or a per-page job like landing/for-clubs were.

Report all five before we fix anything.