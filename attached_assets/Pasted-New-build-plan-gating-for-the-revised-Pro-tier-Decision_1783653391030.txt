New build: plan gating for the revised Pro tier. Decisions made: Free = activity feed, activity logging + personal stats, event discovery/RSVP, and viewing leaderboards. Individual Pro = joining/creating challenges, training analytics + training load, plus Set Goals (not built yet) and AI coaching (not built yet) as coming-soon. Club-scoped features are untouched this session — club Starter keeps its club leaderboards/challenges (known, accepted). Coach's notes stay free.
Master switch: everything gates behind a PLAN_GATES_ENABLED env var (unset/false = no gating anywhere, app behaves exactly as today). We deploy dormant and flip later alongside Stripe live mode.

Map first, then build: identify every individual-scoped endpoint serving (a) challenge join/create/leave actions, (b) training analytics (profile Stats tab data) and training load. Report the list before gating. Explicitly exclude: leaderboard viewing, challenge browsing/viewing, club-scoped challenge and leaderboard functionality, and anything the club dashboard reads for its member monitoring (that's Club Pro's domain, not touched today).
Server-side enforcement: on the mapped endpoints, when PLAN_GATES_ENABLED is true and getUserPlan(userId) returns 'free', respond 403 with a structured body: {"error":"pro_required","feature":"<name>","upgrade":"/billing"}. Same helper-based pattern as requireEventManager. When the flag is false, zero behavioral change — verify that explicitly.
Client-side affordances (also flag-aware — server should inject the flag state into pages): free users see challenges listed but with the join/create actions rendered as honest locked states ("Pro feature — Upgrade") linking to /billing; the profile Stats/analytics area shows a clean upgrade panel instead of the data, never a broken or empty chart. No feature is hidden as if it doesn't exist — locked and labeled, not vanished. Pro users and all users with the flag off see today's behavior.
Test with the flag ON in dev (leave it OFF/unset for production): free test user → challenges join/create blocked server-side (403) and locked in UI, stats tab shows upgrade panel, leaderboards still fully viewable, feed/logging/events untouched; then insert a temporary active pro subscription row for the test user → everything unlocks. Verify flag OFF restores identical-to-today behavior for the same free user. Delete all test rows/users after; report final subscriptions table count.

Report the endpoint map, what was gated, the flag verification both ways, test results, and cleanup confirmation. Touch no pricing copy this session — that's next session.